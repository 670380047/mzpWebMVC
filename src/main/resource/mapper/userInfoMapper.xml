<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.IUserInfoMapper">
    <select id="selectUserInfoAll" resultType="map">
        select * from user_info
    </select>

    <select id="test" resultType="map" parameterType="java.lang.Long">
        select * from user_info where id = #{id} and  my_age = #{routeId}
    </select>
</mapper>